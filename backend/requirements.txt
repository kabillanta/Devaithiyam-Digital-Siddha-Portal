#############################
# Core (SIMPLE_MODE Friendly)
#############################
# Only these are needed when running with SIMPLE_MODE=1 (no LLM / embeddings)
fastapi==0.104.1              # API framework (Pydantic v2 compatible)
uvicorn[standard]==0.24.0     # ASGI server
python-dotenv==1.0.0          # Load .env for SIMPLE_MODE / API keys
pandas==2.1.4                 # CSV loading & preprocessing
numpy==1.24.3                 # Pandas dependency & NaN handling
pydantic==2.5.3               # Data models (explicit pin for reproducibility)

#########################################
# Advanced Mode (SIMPLE_MODE unset / 0)
#########################################
# These enable embeddings + retrieval + Gemini generation
langchain==0.1.0              # LangChain core (version aligned with existing code)
langchain-google-genai==1.0.1 # Gemini chat model wrapper
langchain-community==0.0.10   # Community loaders/vector stores
langchain-huggingface==0.0.1  # HuggingFace embeddings integration
sentence-transformers==2.2.2  # Embedding model backend
faiss-cpu==1.7.4              # Vector similarity search (CPU build)
huggingface_hub==0.19.4       # Model downloads & caching

#########################################
# (Optional) Performance / Serialization
#########################################
# orjson==3.9.10              # (Optional) Faster JSON responses; not required by server.py

# NOTE:
# - Keep versions aligned unless server.py logic updated for breaking changes.
# - Remove advanced block if you want a minimal deterministic deployment.